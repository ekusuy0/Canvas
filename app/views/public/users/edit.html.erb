<div class="container">
  <h4>ユーザー編集</h4>

  <%= form_with model: @user, url: users_information_path, method: :patch do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name, autofocus: true %><br>
    <%= f.submit "変更を保存する" %>
  <% end %>

  <%= link_to "退会する", users_out_check_path %>
</div>