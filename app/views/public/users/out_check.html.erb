<div class="container">
  <h4>退会確認画面</h4>
  退会をクリックすると退会することになります<br>
  <%= link_to "戻る", users_my_page_path %>

  <%= form_with model: @user, url: users_out_path, method: :patch do |f| %>
    <%= f.hidden_field :status, :value => "true" %>
    <%= f.submit "退会する" %>
  <% end %>
</div>